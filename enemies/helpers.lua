local module = {}


-- Get random coordinates, that are not too close to the player
function module.random_coordinates(time)

  -- fix... everything here)
  x = fx_random(950fx)
  y = fx_random(950fx)

  return x, y
end


return module
